services:
  front:
    image: nginx:alpine
    volumes:
      - ./frontend/:/usr/share/nginx/html/
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "9224:80"
    networks:
      - Ativo
    restart: always
networks:
  Ativo: 
    driver: bridge
 